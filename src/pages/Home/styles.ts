import styled from "styled-components";

export const HomeContainer = styled.main`
    width: 100%;
    max-width: 70rem;
    margin: 0 auto;

    display: flex;
    flex-direction: column;
    gap: 1.5rem;
`;
